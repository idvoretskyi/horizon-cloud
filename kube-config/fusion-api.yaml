apiVersion: v1
kind: Service
metadata:
  name: fusion-api
spec:
  type: ClusterIP
  ports:
  - port: 8000
    name: http
    protocol: TCP

---

apiVersion: v1
kind: Endpoints
metadata:
  name: fusion-api
subsets:
- addresses:
  # fusiondev
  - ip: "54.193.31.201"
  ports:
  - port: 8000
