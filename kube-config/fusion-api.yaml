apiVersion: v1
kind: Service
metadata:
  name: api-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    name: http
    protocol: TCP

---

apiVersion: v1
kind: Endpoints
metadata:
  name: api-server
subsets:
- addresses:
  - IP: "54.193.31.201"
  ports:
  - port: 8000
