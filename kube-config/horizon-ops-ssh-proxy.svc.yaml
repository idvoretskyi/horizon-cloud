apiVersion: v1
kind: Service
metadata:
  name: horizon-cloud-ssh-proxy
  labels:
    app: horizon-cloud-ssh-proxy
spec:
  type: LoadBalancer
  selector:
    app: horizon-cloud-ssh-proxy
  ports:
  - port: 22
    name: ssh
    protocol: TCP
