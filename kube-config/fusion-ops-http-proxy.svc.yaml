apiVersion: v1
kind: Service
metadata:
  name: fusion-ops-http-proxy
  labels:
    app: fusion-ops-http-proxy
spec:
  type: LoadBalancer
  selector:
    app: fusion-ops-http-proxy
  ports:
  - port: 80
    name: http
    protocol: TCP
  - port: 443
    name: https
    protocol: TCP
