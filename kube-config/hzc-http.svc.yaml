apiVersion: v1
kind: Service
metadata:
  name: hzc-http
  labels:
    app: hzc-http
spec:
  type: LoadBalancer
  selector:
    app: hzc-http
  ports:
  - port: 80
    name: http
    protocol: TCP
  - port: 443
    name: https
    protocol: TCP
